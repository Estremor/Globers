@model IEnumerable<Globers.Models.AutoresHasLibroModel>

@{
    ViewData["Title"] = "Index";
}

<h1>Index</h1>

<p>
    <a asp-action="Create">Create New</a>
</p>
<table class="table">
    <thead>
        <tr>
            <th>
                @Html.DisplayNameFor(model => model.Autor)
            </th>
            <th>
                @Html.DisplayNameFor(model => model.Libro)
            </th>
            <th></th>
        </tr>
    </thead>
    <tbody>
        @foreach (var item in Model)
        {
            <tr>
                <td>
                    @Html.DisplayFor(modelItem => item.Autor)
                </td>
                <td>
                    @Html.DisplayFor(modelItem => item.Libro)
                </td>
                <td>
                    @Html.ActionLink("Details", "Details", new {  id=item.LibrosIsbn  }) |
                    @Html.ActionLink("Delete", "Delete", new { id=item.LibrosIsbn  })
                </td>
            </tr>
        }
    </tbody>
</table>
